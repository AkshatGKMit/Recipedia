import { StyleSheet } from 'react-native';

import { Colors } from '@themes';

const styles = StyleSheet.create({
  chip: {
    padding: 8,
    borderRadius: 20,
    backgroundColor: Colors.white,
  },
  image: { aspectRatio: 1, height: 20 },
});

export default styles;
